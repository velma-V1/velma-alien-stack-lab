# CURRENT TASK — HOLD

STATUS := HOLD
EXECUTOR := Claude Code

OBJECTIVE:
    preserve Claude usage for the next major Experiment 010 adversarial gate

DO_NOT:
    begin a repository audit
    inspect code broadly
    implement changes
    rerun the prior C/D preflight
    inspect G/H/I

CURRENT_WORK_OWNER := GPT
CURRENT_GPT_WORK:
    independently reproduce/adjudicate prior C/D preflight findings
    restore invalid measurement instruments using TDD
    implement preregistered reporting/rescue instrumentation
    verify with CI

NEXT_CLAUDE_RUN := "FINAL_C_D_RED_TEAM"
TRIGGER:
    GPT reports C/D pre-live blockers resolved
    GPT+user intentionally authorize the narrow review

WHEN_TRIGGERED:
    CURRENT_TASK will be replaced with a surgical pseudocode packet
    scope will contain only final changed files + exact preregistration sections

RETURN_NOW_IF_INVOKED:
    STATUS := HOLD_NO_CLAUDE_RUN_REQUIRED
