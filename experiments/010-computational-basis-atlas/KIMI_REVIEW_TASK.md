# KIMI REVIEW TASK — OPTIONAL POST-FIX RED TEAM

STATUS := OPTIONAL_NOT_A_GATE
MODE := READ_ONLY
REVIEWER := Kimi website
CLAUDE := HOLD
G_H_I_RUNTIME_GATE := CLOSED

PURPOSE:
    preserve this packet as an optional adversarial review tool
    if live C/D evidence later presents a genuinely ambiguous scientific interpretation.

NOT_REQUIRED_FOR:
    pre-live readiness
    GitHub mechanical verification
    authorization to execute frozen local C/D

AUTHORITATIVE_PRELIVE_GATE:
    executable regression tests + GitHub Actions on the shared branch

CURRENT_DECISION:
    do not run this review merely to duplicate green executable checks.
    Use Kimi only when a material claim cannot be settled by code/tests/data/Q14 discrimination.

IF_OPTIONALLY_INVOKED:
    remain read-only
    use Q0-Q36
    cite exact repository evidence
    do not claim to have executed tests from website access
    do not modify the frozen 010-v1 benchmark
    classify unresolvable ambiguity as MORE_DISCRIMINATION_REQUIRED

FORBIDDEN:
    performance-conditioned live pilot
    new control arms in frozen 010-v1
    easier scoring
    altered seeds/tasks/arms/membership
    post-hoc representation dropping
    G/H/I implementation

See GPT_CURRENT_TASK.md for the active execution state.
